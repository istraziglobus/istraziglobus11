name: Daily Vercel Build

on:
  schedule:
    - cron: "1 0 * * *" # svaki dan u 00:01
  workflow_dispatch:

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Vercel Webhook
        run: curl -X POST "https://api.vercel.com/v1/integrations/deploy/prj_TOpurgblQPzNS6QiKNNdDfLJs3Er/kVShWRlcgR"
